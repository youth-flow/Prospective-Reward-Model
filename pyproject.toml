[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prospective-reward-model"
version = "0.3.0"
description = "Exact-delta ProRM training and common-beta NGD evaluation"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Prospective Reward Modeling contributors" }]
dependencies = [
  "numpy>=1.26,<3",
  "pyyaml>=6,<7",
  "safetensors>=0.4,<1",
  "torch>=2.4,<3",
]

[project.scripts]
prorm = "smart_reward.cli:main"

[project.urls]
Repository = "https://github.com/youth-flow/Prospective-Reward-Model"

[project.optional-dependencies]
llm = [
  "accelerate>=1,<2",
  "datasets>=3,<6",
  "peft>=0.14,<1",
  "pyarrow>=17",
  # The pinned Skywork reward model requires a recent Transformers release.
  "transformers>=4.52.3,<5",
]
dev = [
  "pytest>=7.4,<9",
  "pytest-cov>=5,<8",
  "ruff>=0.9,<1",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
pythonpath = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
